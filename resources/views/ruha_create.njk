{% extends 'master' %}

{% block title %}Ruha beküldése{% endblock %}
{% block content %}
  <h1>Ruha beküldése</h1>
  {% if old('errors') %}
  <div class="alert alert-danger" role="alert">Nem sikerült létrehozni a ruhát:
    <ul>
      {% for error in old('errors') %}
      <li>{{ error.message }}</li>
      {% endfor %}
    </ul>
  </div>
  {% endif %}
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <form class="form-horizontal" action="{{ 'do_ruha_create' | route }}" method="POST" enctype="multipart/form-data">
        {{ csrfField }}
        <div class="form-group">
          <label for="inputName" class="col-sm-4 control-label">Ruha neve</label>
          <div class="col-sm-8">
            <input type="text" class="form-control" id="inputName" name="name" placeholder="Ruha neve" value="{{ old('name') }}">
          </div>
        </div>
        <div class="form-group">
          <label for="inputCategory" class="col-sm-4 control-label">Kategória</label>
          <div class="col-sm-8">
            <select id="inputCategory" class="form-control" name="category">
              {% for category in categories %}
              {% if old('category') == category.id %}
              <option value="{{ category.id }}" selected>{{ category.name }}</option>
              {% else %}
              <option value="{{ category.id }}">{{ category.name }}</option>
              {% endif %}
              {% endfor %}
            </select>
          </div>
        </div>
        <div class="form-group">
          <label for="inputanyag" class="col-sm-4 control-label">Anyaga</label>
          <div class="col-sm-8">
            <textarea id="inputanyag" class="form-control" name="anyag">{{ old('anyag') }}</textarea>
          </div>
        </div>
        <div class="form-group">
          <label for="szin" class="col-sm-4 control-label">Szine</label>
          <div class="col-sm-8">
            <textarea id="inputszin" class="form-control" name="szin">{{ old('szin') }}</textarea>
          </div>
        </div>
        <div class="form-group">
          <label for="inputDescription" class="col-sm-4 control-label">Leírás</label>
          <div class="col-sm-8">
            <textarea id="inputDescription" class="form-control" name="description">{{ old('description') }}</textarea>
          </div>
        </div>
        <div class="form-group">
          <label for="inputImage" class="col-sm-4 control-label">Ruha képe</label>
          <div class="col-sm-8">
            <input type="file" id="inputImage" name="image">
          </div>
        </div>
        <div class="form-group">
          <div class="col-sm-offset-4 col-sm-8">
            <button type="submit" class="btn btn-success">Ruha beküldése</button>
          </div>
        </div>
      </form>
    </div>
  </div>
{% endblock %}
